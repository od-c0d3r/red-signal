<header class="p-4 flex justify-between items-center">
    <h1 class="text-3xl font-bold text-white">RedSignal</h1>
    <div class="flex flex-col items-end">
        <span class="font-bold text-white"><%= "Hello, #{current_user.first_name}" if current_user %></span>
        <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white text-xs" if current_user %>
    </div>
</header>
